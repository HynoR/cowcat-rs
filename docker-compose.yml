version: "3.8"

services:
  cowcat-rs:
    build: .
    ports:
      - "8080:8080"
    environment:
      RUST_LOG: info
    volumes:
      - ./config.toml:/app/config.toml:ro
    restart: unless-stopped
